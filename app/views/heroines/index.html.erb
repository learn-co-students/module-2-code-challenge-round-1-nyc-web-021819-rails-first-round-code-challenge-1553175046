<h2>All Heroines</h2>

<!-- attempted code is in README -->

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine_path(heroine.id) %>
    </li>
    <br>
  <% end %>
</ul>

<%= link_to "new", new_heroine_path %>
