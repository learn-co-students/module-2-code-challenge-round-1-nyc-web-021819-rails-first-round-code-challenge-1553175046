<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, "heroines/#{heroine.id}" %>
    </li>
    <br>
  <% end %>
</ul>

<%= button_to "Make new Heroine", new_heroine_path, :method => :get%> <br>

Search By Superpower:
<%= form_tag(heroines_path, method: "get") do %>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag("Search") %>
<% end %>

^ Does not work yet. Sorry :(
