<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine_path(heroine.id) %>
    </li>
    <br>
  <% end %>
</ul>





<!--
  Couldn't find a way to list all the assocatied heroines on the search
  page.  I got to the page fine, but it was blank

  <h3>Search by power:</h3>
<% @powers.each do |power| %>
  <%= button_to power.name, search_path, method: :get %>
<% end  %> -->
