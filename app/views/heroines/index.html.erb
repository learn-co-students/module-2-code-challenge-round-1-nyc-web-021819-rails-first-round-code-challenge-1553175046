<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, "heroines/#{heroine.id}" %>
    </li>
    <br>
  <% end %>
</ul>

<%= button_to "Make new Heroine", new_heroine_path, :method => :get%>
