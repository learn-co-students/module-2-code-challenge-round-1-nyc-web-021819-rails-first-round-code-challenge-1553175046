  <%= form_for @heroine do |f| %>
  <% if @heroine.errors.any? %>
       <div class="" id="error_explanation">
         <h2><%= pluralize(@heroine.errors.count, "error") %> prohibited this post from being saved:</h2>
          <ul>
           <% @heroine.errors.full_messages.each do |msg| %>
           <li><%= msg %></li>
         <% end %>
         </ul>
       </div>
     <% end %>

  <p><%= f.label :name %>
   <%= f.text_field :name %></p>

  <p><%= f.label :super_name %>
     <%= f.text_field :super_name %></p>

  <p><%= f.label :super_power %>
     <%= f.collection_select :super_power, Power.all, :id, :name, prompt:true %>
   </p>


   <p>
     <%= f.submit %>
<% end %>
