 <h1> Create Heroine </h1>

<%= form_for @heroine do |h| %>
  <%= h.label :name %> <br>
  <%= h.text_field :name %> <br>

  <%= h.label :super_name %> <br>
  <%= h.text_field :super_name %> <br>

  <% collection_select(:power_id, @powers, :id, :name) %>
   # I would use this to create a dropdown for the powers
<% end %>
